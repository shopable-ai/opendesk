# Legacy Go runner

This host-side compatibility example is not part of the JavaScript-first user
API. It performs real mouse and keyboard actions after a five-second delay.

For normal automation, use the `.js` examples in `examples/` and run them with
the OpenDesk JavaScript Runtime.
