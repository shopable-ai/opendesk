{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Clawdesk",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${fileDirname}",
            "args": ["-http"]
        }
    ]
}