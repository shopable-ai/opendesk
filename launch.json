{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch TestMonkey",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${fileDirname}",
            "args": ["-http"]
        }
    ]
}