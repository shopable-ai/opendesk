{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch OpenDesk",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "args": ["-http"]
        }
    ]
}
