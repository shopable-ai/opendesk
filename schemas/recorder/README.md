# Recorder schemas

These schemas define the durable OpenDesk Recorder artifacts at schema version
`0.1.0`: the session manifest, append-only Raw Trace events, and deterministic
Flow IR. Runtime evidence and screenshots remain referenced by path rather than
embedded into these documents.
