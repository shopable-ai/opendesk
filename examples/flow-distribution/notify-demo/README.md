# Notify Demo

This public Flow example is intentionally small and low risk. The same `main.js` is used both as a raw JavaScript Flow and as the entry point of the packaged `notify-demo.odflow`.

When the Flow is explicitly run, it shows one OpenDesk-owned Toast and writes one fixed console line. Installing the package never runs `main.js`.

> Install ≠ Run. Seeing a successful install only means the Flow was registered. The Toast is allowed to appear only after an explicit Run.

This example uses canonical `ui.toast()` for execution-owned transient feedback. `ui.notify()` remains its compatibility alias, while global `notify()` is a distinct operating-system notification API and is intentionally not used here.

The generated `flow.json` uses the repository's strict .odflow v1 schema. It does not define capability fields; instead, the signed package includes the strict adjacent `clawdesk.runtime.json` that enables this Flow's `ui` capability. A raw-JavaScript import carries only that one adjacent runtime configuration, verifies its digest before Run, and ignores all other siblings.

## JavaScript smoke test

1. Drag `main.js` into OpenDesk using the existing raw-JavaScript Flow import path.
2. Confirm the Flow is loaded/registered.
3. Explicitly click Run.
4. Confirm the OpenDesk Toast appears and the console contains:
   `OpenDesk Notify Demo: toast requested after explicit run (activation=projectConfig).`

The script does not write files, access the network, invoke shell commands, launch third-party apps, modify system settings, use secrets, or start background work.

## .odflow smoke test

1. Drag `notify-demo.odflow` into OpenDesk.
2. Complete the existing package/signature/trust/install flow.
3. Confirm Flow Runner shows **Notify Demo**.
4. Confirm that no Notify Demo Toast appears merely because installation completed.
5. Explicitly click Run.
6. Confirm the same OpenDesk Toast and fixed console line appear exactly once.

Canceling install, signature/digest rejection, and a normal successful install must all leave `main.js` unexecuted. The stricter marker/malicious/throw-on-run fixtures under `tests/` remain the automated zero-execution proof; this example does not replace them.

## Checked-in artifact provenance

The committed `notify-demo.odflow` was generated by the repository's official `flow pack` command from this exact `main.js` and `clawdesk.runtime.json`, then passed `flow inspect`, `flow verify`, `flow install --trust-flow`, and `flow list` using an isolated app-data root. Installation/listing did not emit the fixed business console line.

The committed package has:

- archive SHA-256: `7e5168273d640a4d3e23adc59e7300e0e8efc1a5b3161e04101379a399236a1e`
- manifest SHA-256: `aa6ede001be7235ccc108908fc086d5a30f4c6fea861b3afb0ba2e7fb2b2205d`
- publisher fingerprint: `582eb7715d66b5e3c3002b9ae90200cec5196106aae5d05bf0071d5b6a06914c`

The checked-in `flow.json` records that archive's generated manifest. It is generated metadata, not a second authoring source; `main.js` plus the documented `flow pack` arguments remain the rebuild inputs.

## Rebuild with the official Flow builder

Run from the repository root. The checked-in [`build.sh`](build.sh) creates an ephemeral key in a fresh system temporary directory, writes only the requested output under `.runtime/`, verifies the result, and removes the key on exit. It never overwrites an output path, stores a private key in the repository, or includes one in the archive.

```bash
./examples/flow-distribution/notify-demo/build.sh .runtime/examples/notify-demo/notify-demo.odflow
unzip -p .runtime/examples/notify-demo/notify-demo.odflow flow.json > .runtime/examples/notify-demo/flow.json
```

The checked-in `notify-demo.odflow` is a convenience artifact for direct drag testing. Rebuilding produces a newly signed package from the same `main.js`; because the development signing key is intentionally ephemeral, the publisher fingerprint changes on rebuild.
