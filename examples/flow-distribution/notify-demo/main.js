'use strict';

notify({
  title: 'OpenDesk Flow Example',
  message: 'Notify Demo is running.\nThis notification appears only after you run the Flow.',
  sound: false,
});

console.log('OpenDesk Notify Demo: notification requested after explicit run.');
