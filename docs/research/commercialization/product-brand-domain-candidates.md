# 产品品牌与域名候选评分

更新时间：2026-09-11

本文件记录 OpenDesk 对外商业产品命名与域名候选的阶段性研究结果。

当前中文品牌方向为 **“不二”**，核心产品主张暂定为：

> 重复的事，人不做第二次。

本文件属于商业化 Research，不代表品牌、商标或域名已经最终冻结。域名注册状态会随时变化；下表中的“可注册/已注册”仅表示 **2026-09-11 实时注册商查询快照**。正式启用前必须重新检查域名状态、续费价格、商标近似风险、主要应用商店账号和社交账号。

## 当前结论

当前最值得进入最终决策的三条路线：

1. **不二 / Untwice**：综合商业完整度最高，`untwice.com` 仍可注册，核心域名资产完整。
2. **不二 / NotTwice**：英文语义最直接，用户第一次看到就容易理解，但 `notwice.com` 已被注册。
3. **不二 / NonBis**：最短的高质量 `.com` 候选之一，文化内涵强，但需要解释 `bis = twice`。

另保留一条 OpenDesk 系备用路线：

4. **OpenDesk X / `opendeskx.com`**：若不建立新的独立商业品牌，而希望延续现有 OpenDesk 技术品牌，可作为高优先级备用；Exact `.com` 在 2026-09-11 查询时仍可注册。`X` 可稳定承接 Cross-App、Cross-Platform、eXecution、eXtensible 四层含义，但该路线仍继承 `OpenDesk` 本身的重名与品牌近似风险，因此不取代前三个独立品牌候选。

若当前必须确定一个主域名，优先级仍为：

```text
untwice.com
> notwice.ai
> nonbis.com
```

OpenDesk 系备用：

```text
opendeskx.com
```

## 域名评分总表

评分不是单纯评价域名长度，而是作为 OpenDesk 商业产品主域名的综合评分。主要考虑：

- 与“不二 / 不重复 / 人不做第二次”品牌语义的匹配度；
- 长度、拼写、发音与记忆成本；
- 国际用户第一次看到时的理解成本；
- `.com` / `.ai` 等核心域名是否可控制；
- 是否适合长期从桌面自动化扩展到 Browser、Mobile、Agent、Workflow；
- 品牌独特性以及被搜索结果、已有软件或其他强含义干扰的风险；
- 后续品牌故事、产品线和营销文案的扩展空间。

| 排名 | 域名 | 评分 | 2026-09-11 状态 | 推荐理由 |
| --- | --- | ---: | --- | --- |
| 1 | **untwice.com** | **98/100** | 可注册 | Exact `.com`；7 字母；`un + twice` 可塑造成“消除第二次重复操作”；原创性高于 NotTwice，同时仍可理解；当前最适合作为长期全球品牌主域名。 |
| 2 | **notwice.ai** | **95/100** | 可注册 | 与“不二 / 不做第二次”语义最直接；第一次看到就容易理解；AI 自动化定位自然；主要缺点是 `notwice.com` 已被注册。 |
| 3 | **nonbis.com** | **94/100** | 可注册 | 6 字母、Exact `.com`；`non bis` 可承接 “not twice / 同一件事不做两次” 的品牌故事；域名资产完整；缺点是普通英语用户需要理解 `bis`。 |
| 4 | **getnotwice.com** | **92/100** | 可注册 | `.com` 可直接注册，符合成熟 SaaS 的 “get + brand” 结构；品牌仍可保持 NotTwice；缺点是并非纯 Exact Brand Domain。 |
| 5 | **untwice.ai** | **91/100** | 可注册 | 若采用 Untwice，是很好的 AI 专用入口；但在 `untwice.com` 可拿的情况下不应替代 `.com` 成为第一核心资产。 |
| 6 | **notwice.app** | **90/100** | 可注册 | 与桌面软件/App 产品属性高度匹配，`NotTwice.app` 读起来自然；适合下载站、产品站或防御性保护。 |
| 7 | **untwice.app** | **89/100** | 可注册 | 与软件产品高度匹配；采用 Untwice 时值得与 `.com` 一起保护。 |
| 8 | **notwice.run** | **89/100** | 可注册 | 与自动执行语义高度契合，`NotTwice.run` 本身具有传播性；不适合作为企业唯一主域名，但适合作为执行产品资产。 |
| 9 | **nonredo.com** | **88/100** | 可注册 | Exact `.com`，容易理解“不重做”；但 `redo` 更偏“第一次做错后重做”，不能完整覆盖周期性重复任务。 |
| 10 | **untwice.run** | **88/100** | 可注册 | Untwice 与 run 的组合很适合作为 Recipe/Execution 服务或产品入口，成本通常也低于 `.ai`；主品牌权重低于 `.com`。 |
| 11 | **notwice.one** | **86/100** | 可注册 | “Not twice. One.” 有很好的品牌故事和“不二 / 一次”内涵；适合营销与品牌保护，不宜作为唯一主站。 |
| 12 | **depeat.com** | **86/100** | 可注册 | 6 字母、Exact `.com`；可解释为 `de + repeat = remove repetition`；缺点是可能被误读为 `defeat` 或被认为是拼写错误。 |
| 13 | **notwice.net** | **83/100** | 可注册 | 简短、干净、可用于品牌保护；但 `.net` 对今天的 AI/软件商业产品主站吸引力明显弱于 `.com/.ai/.app`。 |
| 14 | **norecur.com** | **82/100** | 可注册 | Exact `.com` 且含义接近“不再重复发生”；但技术术语感较强，更像 Runtime/Library 名而不是大众产品品牌。 |
| 15 | **getbuer.com** | **78/100** | 可注册 | 保留 Buer 的短名称优势并能拿到 `.com`；但英语用户无法从 Buer 理解产品价值，而且 Buer 核心域名资产已经较分散。 |
| 16 | **buer.run** | **76/100** | 可注册 | 4 字母极短，`.run` 又符合执行语义；但 `buer.com/.net/.ai/.app/.io` 均已注册，国际品牌解释成本较高。 |
| 17 | **buer.one** | **75/100** | 可注册 | “不二 / One / 唯一”的品牌故事有趣；更适合作为创意或保护域名，不适合作为全球主站。 |

> **OpenDesk 系备用评分：`opendeskx.com` — 95/100（条件评分）**。该评分成立的前提是正式使用 **OpenDesk X** 作为品牌或技术平台名称，使域名与品牌形成 Exact Match；若只是因为 `opendesk.com` 不可用而临时追加字母 `X`，则不应按 95 分评价。

## 三个决赛方案

### A. 不二 / Untwice — 98 分

主域名：**`untwice.com`**

推荐理由：

- 当前 Exact `.com` 可注册；
- 比 NotTwice 更具原创品牌属性；
- `un + twice` 可以被定义为“去掉第二次重复操作”；
- 可以自然形成动词化传播，例如 `Untwice it.`；
- 不限制 Desktop，可覆盖 Browser、Mobile、Agent、Workflow；
- `untwice.ai/.app/.run/.net/.io/.co/.one` 在本轮查询中也可注册，核心资产完整。

可发展的英文表达：

```text
Don't repeat it. Untwice it.
Untwice your work.
```

### B. 不二 / NotTwice — 95 分

主域名候选：**`notwice.ai`**

补充 `.com`：**`getnotwice.com`**

推荐理由：

- 与“不二次 / 人不做第二次”最直接；
- 英语用户几乎不需要品牌教育；
- 可以形成非常清晰的 `Once. Not twice.`；
- `.ai/.app/.run/.net/.io/.one` 当前仍有较完整的保护空间。

主要缺点：

- `notwice.com` 已注册；
- `not twice` 本身也是普通英文短语，搜索独占性弱于优秀原创造词。

### C. 不二 / NonBis — 94 分

主域名：**`nonbis.com`**

推荐理由：

- 仅 6 字母；
- Exact `.com` 当前可注册；
- `non bis` 可以形成 “not twice” 的东西方品牌对应；
- `.ai/.app/.run/.net/.io/.co/.dev/.tech/.one` 在本轮查询中均有较完整的可注册资产；
- 适合走简洁、文化感较强的国际科技品牌路线。

主要缺点：

- 普通英语用户并不知道 `bis` 表示 twice，首次传播需要解释。

## OpenDesk X 备用路线

**品牌/技术平台候选：OpenDesk X**

**主域名：`opendeskx.com`**

**条件评分：95/100**

2026-09-11 实时查询快照：

```text
opendeskx.com      -> 可注册
opendeskx.ai       -> 可注册
opendeskx.app      -> 可注册
opendeskx.io       -> 可注册
opendeskx.run      -> 可注册
getopendeskx.com   -> 可注册
useopendeskx.com   -> 可注册
```

推荐理由：

- `opendeskx.com` 是可直接控制的 Exact `.com`，比 `useopendesk.com` 等前缀域名更完整；
- 可以最大程度继承当前 OpenDesk 仓库、Runtime、SDK 和开发者认知，迁移成本低；
- `X` 不必作为无意义附加字符，可以冻结为多层产品语义：
  - **Cross-App**：跨桌面应用和业务系统执行；
  - **Cross-Platform**：Windows、macOS 以及未来其他执行端；
  - **eXecution**：把 Agent/AI 的意图转化为真实软件操作；
  - **eXtensible**：JavaScript、Recipe、Runtime API、Custom UI 等扩展能力；
- 可进一步形成 `OpenDesk Studio`、`OpenDesk Run`、`OpenDesk Runtime` 等技术产品线，而 OpenDesk X 作为上层技术品牌。

主要限制：

- 该方案仍完整继承 `OpenDesk` 字样，因此无法消除已有同名/近似 OpenDesk 软件、AI、自动化项目造成的搜索、商标和品牌混淆风险；
- `X` 的四层解释必须真正写入品牌规范并长期一致使用，否则容易退化成“为了拿 `.com` 临时加一个 X”；
- OpenDesk X 更适合作为**现有技术品牌延续方案/备用方案**，不自动优先于具有独立品牌资产的“不二 / Untwice”。

当前定位建议：

```text
商业用户品牌候选：不二 / Untwice
技术平台：OpenDesk / OpenDesk X（备用升级路线）

如果最终采用双层品牌：
Untwice
  -> Powered by OpenDesk X
```

## Buer 路线判断

`Buer` 的优势非常明确：只有 4 个字母，并且与中文“不二”的拼音形式直接相关。

但作为国际主品牌，目前存在三个结构性问题：

```text
buer.com  -> 已注册
buer.net  -> 已注册
buer.ai   -> 已注册
buer.app  -> 已注册
buer.io   -> 已注册
```

同时，海外用户第一次看到 `Buer` 无法自然理解“不重复 / 不做第二次 / automation”的产品价值。因此，不能仅因为字符更少就认为品牌传播成本更低。

若保留 Buer，更适合作为：

- 中文“不二”的罗马字/拼音标识；
- 中文品牌故事中的文化元素；
- 非核心域名或局部产品命名候选。

当前不建议优先于 Untwice / NotTwice / NonBis。

## 建议注册策略

若采用 **Untwice**，优先顺序：

```text
P0  untwice.com
P0  untwice.ai
P1  untwice.app
P1  untwice.run
P2  untwice.net / untwice.io / untwice.co / untwice.one
```

若采用 **NotTwice**，优先顺序：

```text
P0  notwice.ai
P0  getnotwice.com
P1  notwice.app
P1  notwice.run
P2  notwice.net / notwice.io / notwice.one
```

若采用 **NonBis**，优先顺序：

```text
P0  nonbis.com
P0  nonbis.ai
P1  nonbis.app
P1  nonbis.run
P2  nonbis.net / nonbis.io / nonbis.co / nonbis.one
```

若保留 **OpenDesk X** 作为备用技术品牌，优先顺序：

```text
P0  opendeskx.com
P1  opendeskx.ai
P1  opendeskx.app
P2  opendeskx.run / opendeskx.io
```

不建议因为首年促销价格很低就大量注册弱候选。优先控制：

1. Exact `.com`；
2. 与产品属性高度一致的 `.ai` / `.app`；
3. 有强品牌语义的 `.run` / `.one`；
4. 只在品牌基本冻结后再做广泛防御性注册。

## 当前中文品牌主张

当前保留以下文案层级，后续品牌研究可继续优化：

```text
中文品牌：不二

产品主张：重复的事，人不做第二次。
传播短句：重复事，不二次。
产品文案：做一次，剩下的交给不二。
品牌使命：重复归机器，创造归人。
```

这些文案目前是 Naming Research 的候选，不应视为已经对外发布的正式品牌规范。

## 使用与更新规则

- 域名“可注册”状态必须附查询日期，不能长期当作静态事实；
- 决策前重新查询 P0 域名，不以搜索引擎“搜不到网站”替代注册商/RDAP 状态；
- `.ai` 等后缀必须核对实际结算价和续费价，促销/接口显示价格不能直接视为长期成本；
- 域名可注册不代表商标可使用；正式发布前需要补中国及主要国际市场的商标近似检索；
- Naming Research 不自动触发代码、包名、仓库名、App ID 或现有 OpenDesk Runtime 品牌迁移；
- 在商业产品名称最终冻结前，**OpenDesk** 继续作为当前技术平台、Runtime、SDK 和仓库名称。
