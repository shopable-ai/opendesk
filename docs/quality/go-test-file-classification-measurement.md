# Desktop Measurement P0–P4 Go 测试增量分类

本文件是 `docs/quality/go-test-file-classification.md` 的受审增量清单，只登记 Desktop Measurement P0→P4、Measurement 入口和 Recorder evidence 集成新增的 Go 测试。历史 151 项基线仍由主清单负责；`scripts/audit_test_architecture.js` 会把本文件追加到主清单后统一解析，因此这些行都计入现有 `## 增量登记` 之后的 reviewed rows。

所有条目都保持真实测试边界：它们依赖同包私有状态、native host source contract、package-relative fixture/helper 或产品入口装配，因此保留为 `KEEP_PACKAGE`；真实 macOS / Windows 桌面资格仍由 `tests/desktop-measurement/qualification-manifest.json` 与本地 Codex live gate 单列，不能由这些确定性测试冒充。

| 文件 | 主处置 | 私有依赖 | 主要职责 | 外部依赖 | 断言价值 | 结论与证据 |
| --- | --- | --- | --- | --- | --- | --- |
| `automation/global_shortcut_product_test.go` | `KEEP_PACKAGE` | 是：产品级全局快捷键注册、回调投递与同包 Runtime owner seam | Desktop Measurement 全局快捷键从注册到产品 action 的唯一入口与冲突处理 | 内存 shortcut backend、Goja/EventLoop fixture；不触发真实系统快捷键 | 防止产品快捷键与普通脚本 owner 冲突、重复注册或 callback 路由到错误 action | 这是 Automation host 的私有产品装配白盒；真实 `Cmd/Ctrl+Shift+M` 仍由目标系统 live qualification 证明。 |
| `cmd/opendesk/app_measurement_shortcut_test.go` | `KEEP_PACKAGE` | 是：`package main` 的 Measurement shortcut owner、App Mode action bridge 与 lifecycle helper | 产品 App Mode 启动、重复入口、退出清理和 shortcut 到唯一 Measurement Session 的转发 | 内存 App Shell/shortcut adapter 与 context；不创建真实系统热键 | 防止 shortcut 创建第二 Session、App 退出后遗留注册或转发越过产品 action | CLI 产品装配必须同包验证；真实系统级快捷键和焦点恢复由本地 macOS/Windows 验收单列。 |
| `internal/measurementshortcut/shortcut_test.go` | `KEEP_PACKAGE` | 是：internal shortcut canonical accelerator、平台显示字符串与 normalization helper | `CommandOrControl+Shift+M` 的单一来源、macOS/Windows 展示和非法变体拒绝 | 纯内存字符串与平台参数 | 防止菜单、Recorder tooltip、native registration 各自硬编码并发生组合漂移 | internal 单一事实源白盒；它不声明真实 OS 已成功注册快捷键。 |
| `pkg/appshell/localization_menu_test.go` | `KEEP_PACKAGE` | 是：`nativeMenuItem`、产品语言选择状态与 private menu composition seam | Localization 切换后产品菜单文本、checkmark 和 Measurement 快捷键标签保持 action identity | 临时 locale preference/catalog 与内存 manifest；不创建真实 tray/menu | 防止语言切换改变稳定 action、丢失选择状态或 Measurement 标签与快捷键单一来源分叉 | App Shell private presentation seam；原生菜单刷新与系统语言体验另做 live。 |
| `pkg/customui/measurement_host_contract_test.go` | `KEEP_PACKAGE` | 是：CustomUI native host 源文件、package-relative bridge resource 与 Measurement event protocol seam | macOS/Windows `measurement.key`、Tab、Alt phase、1–4、R/I 等 host 协议静态一致性 | 仓库 `machost`/`winhost` 受审源码；不创建原生窗口 | 防止一端遗漏 keyup/FlagsChanged 或 Measurement command 名称漂移而仅在某平台失效 | Native host bridge 属于 CustomUI 私有协议边界；真实键盘焦点和系统输入仍需物理 OS qualification。 |
| `pkg/measurement/authoring_test.go` | `KEEP_PACKAGE` | 是：`evidenceFixture`、`pathHasSuffix` 与 Measurement authoring package 内部 fixture seam | semantic-first、Human-to-Recipe handoff、task package persistence 与不安全 artifact ref 拒绝 | `t.TempDir()`、固定时间和合成 Evidence | 防止 Measurement 覆盖 semantic locator、handoff 脱离现有 task package 或路径穿越 | P3 authoring 组合白盒；真实 Recorder/Human/Agent task package 由本地端到端验收证明。 |
| `pkg/measurement/capture_mapping_matrix_test.go` | `KEEP_PACKAGE` | 是：canonical CaptureMapping 与同包矩阵 fixture/helper | 1.0/1.25/1.5/2.0、负 origin、左/上显示器、desktop hole 和 display 分离 | 固定逻辑/像素几何，无真实显示器 | 防止 DPI 换算、负坐标或跨 display 坐标被静默解释到错误 capture space | P2 坐标合同白盒；mixed-DPI 物理屏幕仍保持 `NOT_RUN` 直到真机验证。 |
| `pkg/measurement/evidence_test.go` | `KEEP_PACKAGE` | 是：`evidenceFixture`、artifact path/hash helper 与 package-private evidence validation seam | Evidence 序列化、snapshot hash/dimensions、reload、tamper/identity 检查和 task package 路径 | 合成 PNG、`t.TempDir()`、固定 Result/Reference | 防止 evidence 与 snapshot 脱节、reload 接受篡改或另起孤立 artifact 目录 | P3 持久化白盒；同一 canonical Result/Mapping 仍是唯一 Geometry 来源。 |
| `pkg/measurement/interaction_test.go` | `KEEP_PACKAGE` | 是：Region edit handle、HUD/Micro placement 与 private deterministic geometry helper | body drag、八方向 handle、display clamp、HUD 四角避让和 Micro 边缘翻转 | 固定 Rect/Point/CaptureMapping，无 UI host | 防止 resize 改错 edge、区域越界或 HUD/Micro 覆盖策略出现非确定性 | P0/P1 交互几何白盒；原生拖拽流畅度和视觉遮挡仍由 live qualification 判断。 |
| `pkg/measurement/model_additional_test.go` | `KEEP_PACKAGE` | 是：Measurement model 的 package-private validation 与额外边界 fixture | reverse drag、near-zero、negative coordinate、spacing 和 canonical result 边界补充 | 合成 geometry/image fixture | 防止边界 case 通过隐式裁剪、零维结果或符号漂移产生不一致输出 | 与 `model_test.go` 共同冻结唯一 Result/Geometry 合同，不建立平台平行模型。 |
| `pkg/measurement/product_contract_test.go` | `KEEP_PACKAGE` | 是：CandidateDescriptor、TwoLevelReferences、SnapshotToken 与 private reliability/containment helper | 两级 Reference、candidate hierarchy、stable relocation 与 snapshot generation 合同 | 固定窗口/候选/目标几何 | 防止 estimated visual candidate 被提升为 semantic parent、Reference 层级泛滥或 stale token 被接受 | 产品 Evidence 合同白盒；真实 AX/UIA candidate 质量由 Recorder/平台验收补证。 |
| `pkg/measurement/provenance_test.go` | `KEEP_PACKAGE` | 是：Evidence provenance derivation 与同包 origin normalization seam | manual、recorder、accessibility、uia、window、capture、measurement、agent 来源记录 | 合成 MeasurementEvidence | 防止 geometry、Reference、pixel 或 candidate 的真实来源在 handoff 中被抹平或伪造 | P3 provenance 白盒，是 Repair/Qualification 可解释性的基础，不代表外部感知 backend live PASS。 |
| `pkg/measurement/qualification_test.go` | `KEEP_PACKAGE` | 是：`qualifyReference`、geometry/pixel tolerance helper 与 Evidence fixture seam | Reference identity、target drift 和 frozen pixel plausibility qualification | 固定 observation、Result 和 policy | 防止 Reference 换窗、几何超阈值或颜色明显漂移仍被标记 qualified | Qualification 只消费证据并 fail closed；不会把 Measurement 变成 pixel-only locator。 |
| `pkg/measurement/repair_test.go` | `KEEP_PACKAGE` | 是：repair executor/verifier test adapters、history path 和 private append/load helper | failure eligibility、RepairRequest、execution/verification 双门、history persistence | 内存 adapters、`t.TempDir()`、固定时间 | 防止 permission/OCR 等原始故障误触发 Measurement，或只执行成功就提前 qualified | P4 repair 状态机白盒；正式 Recipe promotion 仍是后续显式 authoring 决策。 |
| `pkg/measurement/repair_workflow_test.go` | `KEEP_PACKAGE` | 是：Calculator repair fixture、`deepCopyMap` 与 package-local candidate constraint seam | deterministic Calculator geometry drift → new evidence → retry → business verification → qualified | 纯内存 Calculator target/display fixture | 防止 proposed repair 静默修改黄金 Recipe，且验证双 PASS 之前状态不能升级 | P4 workflow proof 复用现有 RepairExecutor/Verifier 接口；真实 Calculator UI 由本地 Codex 再跑。 |
| `pkg/measurement/session_interaction_test.go` | `KEEP_PACKAGE` | 是：`activeSession`、`handleKey`、`handle`、regionHandle 与 session fixture | Esc 严格层级、I、Alt down/up、Region body/SE 编辑、Tab 真实 candidate | MemoryDriver、合成 Capture 与 fake Clipboard | 防止内层 Esc 一次退出整个 Session、modifier 状态泄漏或交互时重建 surface | P0 事件状态机白盒；native host event delivery 由 CustomUI contract 和真机共同验证。 |
| `pkg/measurement/session_layout_test.go` | `KEEP_PACKAGE` | 是：Measurement Surface spec builder、固定 control IDs 与 package-local layout helper | Frozen layer、overlay、HUD、Micro、Toolbar、Inspector/Copy Menu 的层级和默认可见性 | 内存 WindowSpec，无真实窗口 | 防止产品 Surface 退回大型 debug UI、控件 ID 重复或默认展示完整 JSON/PID | P0 layout contract 白盒；Prototype 是视觉/交互 Oracle，真实 Native window 另验。 |
| `pkg/measurement/session_product_test.go` | `KEEP_PACKAGE` | 是：`activeSession`、`currentWindow`、snapshot acceptance 和 product HUD helper | ADJUSTING/refreeze、同一 surface、generation invalidation、Frozen RGB、两组 margin HUD | MemoryDriver、合成 capture、fake clipboard | 防止更新画面复用 stale Snapshot、调整界面显示旧冻结层或 duplicate entry 新建 surface | P0/P1 产品生命周期白盒；系统截图排污和真实上下文恢复由本地验收确认。 |
| `pkg/measurement/structured_test.go` | `KEEP_PACKAGE` | 是：StructuredMeasurementData 与 legacy raw Result compatibility 的同包 serializer seam | 三档结构化输出 schema、canonical Result 嵌套和 backward-compatible decode | 固定 Result/JSON，无外部系统 | 防止第三档输出丢 `captureMapping`/Reference/evidence 或 schema 与 canonical Result 分叉 | Machine-consumable output 与 Human-readable output 共享同一 Result，不建立 UI 专属模型。 |
| `pkg/measurement/surface_overlay_test.go` | `KEEP_PACKAGE` | 是：canonical overlay renderer、asset writer 与 package-private render fingerprint/helper | Reference/Target/guides/八 handles 的透明 overlay 生成、dirty update 和 asset cleanup | 合成 PNG、临时文件和固定 geometry | 防止 pointer 更新退回重建窗口、overlay 几何漂移或退出后遗留临时资产 | P0/P1 单 Surface 渲染白盒；真实 GPU/WebView/native repaint 性能仍由目标平台测量。 |
| `pkg/recorder/measurement_evidence_test.go` | `KEEP_PACKAGE` | 是：Recorder action/LocatorCandidate 内部 evidence ref attachment 与 normalization seam | 可选 Measurement Evidence 挂接、semantic locator 保留、跨平台安全相对路径校验 | 固定 Recorder action/evidence refs，无真实录制输入 | 防止 Measurement 变成每步强制依赖、覆盖现有 semantic evidence 或接受 Windows/Unix 路径穿越 | P3 Recorder integration 白盒；真实 Recorder → Measurement artifact 仍由本地 Codex 端到端验证。 |
