# WeChat golden fixtures

This directory contains only reviewed, frozen test fixtures and their registry.

- `replay/replay_case.json` is a reusable test contract.
- `replay/replay_result.json` is historical evidence and should not drive a new action.
- Candidate samples belong in `.runtime/candidates/` until reviewed.
- Runtime screenshots, logs, and derived analysis belong in `.runtime/runs/`.
