# Drawing test tools

The code in this directory exercises Go-side drawing helpers. It is not part of
the JavaScript Runtime user API.

Run the visual demo from the repository root:

```sh
go run ./tests/drawing/tools/demo
```

The generated PNG is written to
`.runtime/tests/drawing/demo/drawing_demo.png`.
